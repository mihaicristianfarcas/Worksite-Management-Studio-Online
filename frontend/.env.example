# Example environment variables for the frontend (Vite)
# Copy to .env or .env.development/.env.production and adjust values

# Base API URL for the backend (no trailing slash except when needed)
# Example: https://api.example.com/api or http://localhost:8080/api
VITE_API_URL=http://localhost:8080/api

# Notes:
# - Use import.meta.env.VITE_API_URL in your code to access this value at runtime.
# - Several services in the codebase currently use a hard-coded API URL string
#   (e.g. in src/api/services/*.service.ts). It's recommended to replace those
#   constants with import.meta.env.VITE_API_URL for easier configuration.
# - Vite's `loadEnv` will automatically load .env files (e.g. .env.development).